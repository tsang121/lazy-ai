{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx serve -p $PORT .",
    "healthcheckPath": "/",
    "healthcheckTimeout": 60
  }
}
